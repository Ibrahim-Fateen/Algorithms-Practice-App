<!DOCTYPE html>
<html>
<head>
  <title>Algorithm Practice</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/python/python.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/theme/monokai.min.css">
  <%= javascript_importmap_tags %>
</head>

<body>
<nav class="bg-gray-800 text-white p-4">
  <div class="container mx-auto flex justify-between items-center">
    <%= link_to 'AlgoPractice', root_path, class: "text-xl font-bold" %>
    <div class="space-x-4">
      <% if user_signed_in? %>
        <%= link_to 'Problems', problems_path %>
        <%= link_to 'Dashboard', dashboard_path %>
        <% if current_user.admin? %>
          <%= link_to 'Admin Problems', admin_problems_path, class: 'text-gray-700 hover:text-blue-600' %>
          <%= link_to 'Admin Weeks', admin_weeks_path, class: 'text-gray-700 hover:text-blue-600' %>
          <%= link_to 'Admin Users', admin_users_path, class: 'text-gray-700 hover:text-blue-600' %>
        <% end %>
        <%= button_to 'Sign Out', destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path %>
        <%= link_to 'Sign Up', new_user_registration_path %>
      <% end %>
    </div>
  </div>
</nav>

<div class="container mx-auto mt-8 px-4">
  <% if notice %>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
      <%= notice %>
    </div>
  <% end %>

  <% if alert %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
      <%= alert %>
    </div>
  <% end %>

  <%= yield %>
</div>
</body>
</html>